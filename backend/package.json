{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon --watch src --ext js,graphql --exec babel-node  index.js",
    "build": "babel src --out-dir build && shx cp .env build 2>/dev/null || : && shx cp src/schema/schema.graphql build/schema",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-datasource-mongodb": "^0.2.7",
    "apollo-server": "^2.16.1",
    "apollo-server-express": "^2.25.0",
    "express": "^4.17.1",
    "graphql": "^15.3.0",
    "mongodb": "^3.6.0"
  }
}
